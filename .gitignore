# build directories
build/
build-*/
CMakeCache.txt
CMakeFiles/

# doc directory to discourage making changes in commit, since the CI updates the doxygen docs
docs/

# language server files
.cache/
compile_commands.json
